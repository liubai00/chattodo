services:
  chattodo-api:
    build: ../server
    image: chattodo-api
    container_name: chattodo-api
    # Bound to localhost only — reached via nginx /todo/api/, never exposed directly.
    ports:
      - "127.0.0.1:8788:8787"
    volumes:
      - chattodo-data:/data   # 旧 SQLite 库 → 首次启动迁移到 Postgres 的来源
    environment:
      - REDIS_URL=redis://chattodo-redis:6379
      - DATABASE_URL=postgres://chattodo:chattodo_internal_pw@chattodo-postgres:5432/chattodo
    depends_on:
      chattodo-redis:
        condition: service_healthy
      chattodo-postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8787/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }

  # 主数据库（独立卷，仅 compose 内网可达，不发布端口 → 与你现有 PG / 其它应用零干扰）。
  chattodo-postgres:
    image: postgres:16-alpine
    container_name: chattodo-postgres
    environment:
      POSTGRES_USER: chattodo
      POSTGRES_PASSWORD: chattodo_internal_pw
      POSTGRES_DB: chattodo
    volumes:
      - chattodo-pgdata:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chattodo -d chattodo"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }

  # 实时事件总线（pub/sub 专用，不持久化）。仅 compose 内网可达，不发布任何端口。
  chattodo-redis:
    image: redis:7-alpine
    container_name: chattodo-redis
    command: ["redis-server", "--save", "", "--appendonly", "no", "--maxmemory", "64mb", "--maxmemory-policy", "allkeys-lru"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 3s
      retries: 3
    logging:
      driver: json-file
      options: { max-size: "5m", max-file: "2" }

volumes:
  chattodo-data:
  chattodo-pgdata:
