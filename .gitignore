node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local archive (design bundle) - keep the extracted todo/ folder, not the zip
/todo.zip

# 打包 / 部署产物（本地生成，勿提交 git）
.deploy-tmp.tar.gz
.nginx-tmp.conf

# 部署凭证（本地专用，勿提交 git）
.env
!.env.example
.deploy-secret
.ssh/

# IDE / editor
.idea/

# runtime logs & crash dumps
*.log
*.stackdump

# local tools (cloudflared binary)
/tools/cloudflared.exe

# 本地 PGlite 开发库
/data/pgdata-dev
